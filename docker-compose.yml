services:
  postgres:
    image: postgres:16
    container_name: scouts-postgres

    environment:
      POSTGRES_USER: scouts
      POSTGRES_PASSWORD: scouts123
      POSTGRES_DB: scoutsdb

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: